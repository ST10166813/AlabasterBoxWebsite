@{
    ViewData["Title"] = "About Us";
}

@section Styles {
    <style>
        /* RESET */
        * { box-sizing: border-box; margin: 0; padding: 0; }

        html, body {
            font-family: 'Poppins', 'Segoe UI', sans-serif;
    background: url('../images/cross7.png') center -10% / cover no-repeat fixed;
            background-color: #000;
            color: #fff;
            min-height: 100vh;
            overflow-x: hidden;
        }

        a { text-decoration: none; color: inherit; }

        /* CINEMATIC OVERLAY */
        body::before {
            content: "";
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.35); /* same overlay as Events pages */
            z-index: 0;
        }

        /* MAIN SECTION */
        .about-section {
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 5.4rem 1.35rem;
            position: relative;
            z-index: 1;
        }

        /* GLASS CARD */
        .about-card {
            position: relative;
            z-index: 2;
            background: rgba(0, 0, 0, 0.55); /* darker, cinematic card */
            border-radius: 2.25rem;
            padding: 2.7rem;
            backdrop-filter: blur(18px) saturate(180%);
            border: 1px solid rgba(255, 215, 0, 0.25);
            box-shadow: 0 22px 54px rgba(0,0,0,0.55);
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 2.25rem;
            max-width: 1035px;
            width: 100%;
            opacity: 0;
            transform: translateY(27px);
            transition: all 1.2s ease;
        }

        .about-card.visible {
            opacity: 1;
            transform: translateY(0);
        }

        /* TITLE */
        .about-card h1 {
            grid-column: 1 / -1;
            text-align: center;
            font-size: 3.6rem;
            font-weight: 900;
            background: linear-gradient(90deg, #FFD700, #fff, #FFD700);
            background-size: 200% auto;
            color: transparent;
            background-clip: text;
            -webkit-background-clip: text;
            animation: shimmer 6s linear infinite, fadeUp 1s ease forwards;
            text-shadow: 0 0 22px rgba(255,215,0,0.4);
            letter-spacing: 1.8px;
            margin-bottom: 0.9rem;
        }

        @@keyframes shimmer {
            0% { background-position: 0% 50%; }
            100% { background-position: 200% 50%; }
        }

        @@keyframes fadeUp {
            0% { opacity: 0; transform: translateY(18px); }
            100% { opacity: 1; transform: translateY(0); }
        }

        /* IMAGES */
        .about-images {
            display: grid;
            gap: 1.17rem;
            align-content: start;
        }

        .about-images img {
            width: 100%;
            border-radius: 1.35rem;
            object-fit: cover;
            transition: all 0.45s ease;
            filter: brightness(0.95) contrast(1.05);
            box-shadow: 0 9px 22px rgba(0,0,0,0.4);
        }

        .about-images img:first-child { height: 225px; }
        .about-images img:last-child { width: 225px; height: 225px; }

        .about-images img:hover {
            transform: scale(1.045) rotate(1deg);
            box-shadow: 0 0 36px rgba(255,215,0,0.6);
            filter: brightness(1.1);
        }

        /* CONTENT */
        .about-content {
            animation: fadeInContent 1.8s ease forwards;
        }

        @@keyframes fadeInContent {
            0% { opacity: 0; transform: translateX(27px); }
            100% { opacity: 1; transform: translateX(0); }
        }

        .about-content h2 {
            font-size: 2.16rem;
            font-weight: 800;
            color: #fff;
            margin-bottom: 1.08rem;
            text-shadow: 0 3.6px 10.8px rgba(0,0,0,0.7);
            position: relative;
        }

        .about-content h2::after {
            content: "";
            width: 76.5px;
            height: 3.6px;
            background: linear-gradient(to right, #00c6ff, #0072ff);
            display: block;
            margin-top: 9px;
            border-radius: 2.7px;
        }

        .about-content p {
            font-size: 1.17rem;
            line-height: 1.8;
            color: #f1f1f1;
            margin-bottom: 1.35rem;
            text-shadow: 0 1.8px 5.4px rgba(0,0,0,0.5);
        }

        .about-content p.font-weight-bold {
            font-weight: 700;
            color: #fff;
            text-shadow: 0 0 9px rgba(255,255,255,0.3);
        }

        /* RESPONSIVE */
        @@media (max-width: 992px) {
            .about-card {
                grid-template-columns: 1fr;
                text-align: center;
                padding: 2.1rem;
            }

            .about-images img:first-child { height: 180px; }
            .about-images img:last-child { width: 180px; height: 180px; margin: 0 auto; }
            .about-content { margin-top: 1.8rem; }
        }

        @@media (max-width: 768px) {
            .about-card h1 { font-size: 2.7rem; }
            .about-content h2 { font-size: 1.7rem; }
            .about-content p { font-size: 1rem; }
        }
    </style>
}

@section Scripts {
    <script>
        // Fade-in on scroll
        const observer = new IntersectionObserver(entries => {
            entries.forEach(entry => {
                if (entry.isIntersecting) entry.target.classList.add('visible');
            });
        }, { threshold: 0.2 });

        document.querySelectorAll('.about-card').forEach(el => observer.observe(el));
    </script>
}

<!-- HTML -->
<div class="about-section">
    <div class="about-card">
        <h1>About Us</h1>
        <div class="about-images">
            <img src="~/images/team-member.jpg" alt="Team Praying Together">
            <img src="~/images/404.png" alt="Community Outreach">
        </div>
        <div class="about-content">
            <h2>Who We Are</h2>
            <p>Alabaster Box Anointing Ministries is a Christ-centered organization dedicated to spreading the love, hope, and healing power of Jesus Christ to communities near and far. Rooted in <strong>Mark 16:15</strong>, our mission is to go into all the world and share the Good News through outreach, prayer, charity, and discipleship.</p>
            <p>Founded with a heart for service and a passion for transformation, we believe that every person matters and that through faith, lives can be restored and empowered. Whether we’re feeding the hungry, praying for the brokenhearted, or equipping others with the Word of God, our purpose remains the same: to serve with compassion and walk in the anointing of the Holy Spirit.</p>
            <p class="font-weight-bold">Join us as we continue to pour out love like the alabaster jar — selflessly and wholeheartedly — for the glory of God.</p>
        </div>
    </div>
</div>
