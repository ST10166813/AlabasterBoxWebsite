@* @{
    ViewData["Title"] = "Donate";
}

@section Styles {
    <style>
        /* RESET */
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        html, body {
            font-family: 'Poppins', 'Segoe UI', sans-serif;
            background: url('../images/cross7.png') center -10% / cover no-repeat fixed;
            background-color: #000;
            color: #fff;
            min-height: 100vh;
            overflow-x: hidden;
        }

        a {
            text-decoration: none;
            color: inherit;
        }

        /* CINEMATIC OVERLAY */
        body::before {
            content: "";
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.35);
            z-index: 0;
        }

        /* PAGE LAYOUT */
        .donate-hero {
            position: relative;
            background: url('../images/Donate.jpg') center center / cover no-repeat;
            width: 100%;
            min-height: 320px;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            margin-bottom: 2.8rem;
            z-index: 1;
        }

            .donate-hero::after {
                content: "";
                position: absolute;
                inset: 0;
                background: rgba(0,0,0,0.45);
                z-index: 1;
                border-bottom: 1px solid rgba(255,215,0,0.06);
            }

            .donate-hero .hero-content {
                position: relative;
                z-index: 2;
                padding: 2.2rem 1rem;
                color: #fff;
            }

            .donate-hero h1 {
                font-size: 3rem;
                font-weight: 900;
                margin-bottom: 0.5rem;
                background: linear-gradient(90deg, #FFD700, #fff, #FFD700);
                background-size: 200% auto;
                color: transparent;
                -webkit-background-clip: text;
                background-clip: text;
                text-shadow: 0 0 18px rgba(255,215,0,0.25);
                animation: shimmer 6s linear infinite;
            }

            .donate-hero p {
                font-size: 1.05rem;
                color: #f4f4f4;
                max-width: 900px;
                margin: 0.6rem auto 1rem auto;
                text-shadow: 0 1.6px 4.8px rgba(0,0,0,0.55);
                line-height: 1.6;
            }

            .donate-hero .hero-cta {
                display: inline-block;
                margin-top: 0.9rem;
                padding: 0.9rem 1.8rem;
                font-size: 1rem;
                color: #000;
                background: linear-gradient(135deg, #FFD700, #FFAA00);
                border-radius: 999px;
                font-weight: 800;
                box-shadow: 0 10px 28px rgba(255, 170, 0, 0.25);
            }

        @@keyframes shimmer {
            0% {
                background-position: 0% 50%;
            }

            100% {
                background-position: 200% 50%;
            }
        }

        /* MAIN CARD */
        .donate-section {
            display: flex;
            justify-content: center;
            padding: 2.6rem 1.2rem 4rem 1.2rem;
            z-index: 1;
            position: relative;
        }

        .donate-card {
            width: 100%;
            max-width: 1100px;
            display: grid;
            grid-template-columns: 1fr 420px;
            gap: 2rem;
            background: rgba(0,0,0,0.55);
            border-radius: 1.35rem;
            padding: 2.4rem;
            border: 1px solid rgba(255,215,0,0.12);
            backdrop-filter: blur(18px) saturate(160%);
            box-shadow: 0 26px 60px rgba(0,0,0,0.55);
            opacity: 0;
            transform: translateY(26px);
            transition: all 1s ease;
        }

            .donate-card.visible {
                opacity: 1;
                transform: translateY(0);
            }

        /* LEFT CONTENT */
        .donate-content h2 {
            font-size: 2.1rem;
            font-weight: 900;
            margin-bottom: 0.9rem;
            color: #fff;
            text-shadow: 0 3px 10px rgba(0,0,0,0.6);
        }

        .donate-content p {
            font-size: 1.05rem;
            color: #f1f1f1;
            line-height: 1.75;
            margin-bottom: 1rem;
        }

        .donate-image {
            width: 100%;
            border-radius: 1rem;
            object-fit: cover;
            display: block;
            margin: 1.15rem 0 0.75rem 0;
            box-shadow: 0 12px 36px rgba(0,0,0,0.45);
        }

        .donate-note {
            font-size: 0.98rem;
            color: #fff;
            background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
            padding: 0.9rem;
            border-radius: 0.8rem;
            border: 1px solid rgba(255,215,0,0.06);
            box-shadow: 0 8px 20px rgba(0,0,0,0.35);
        }

        /* RIGHT: DONATION OPTIONS */
        .donate-options {
            display: flex;
            flex-direction: column;
            gap: 1.35rem;
        }

        .option-card {
            background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
            border-radius: 1rem;
            padding: 1.25rem;
            border: 1px solid rgba(255,215,0,0.08);
            box-shadow: 0 12px 30px rgba(0,0,0,0.45);
        }

            .option-card h3 {
                font-size: 1.25rem;
                font-weight: 800;
                margin-bottom: 0.6rem;
                color: #FFD700;
            }

            .option-card p {
                font-size: 0.98rem;
                color: #eee;
                line-height: 1.6;
                margin-bottom: 0.7rem;
            }

        /* BANK DETAILS */
        .bank-details {
            background: rgba(0,0,0,0.35);
            border-radius: 0.75rem;
            padding: 0.9rem;
            color: #fff;
            border: 1px dashed rgba(255,215,0,0.12);
        }

        .bank-line {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 0.8rem;
            margin-bottom: 0.5rem;
            font-size: 0.98rem;
        }

            .bank-line strong {
                color: #ffd;
                font-weight: 700;
            }

        .copy-btn {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            padding: 6px 10px;
            font-size: 0.9rem;
            border-radius: 999px;
            background: rgba(255,215,0,0.95);
            color: #000;
            border: none;
            cursor: pointer;
            font-weight: 700;
            box-shadow: 0 8px 20px rgba(255,215,0,0.12);
        }

        .small-muted {
            font-size: 0.85rem;
            color: #ddd;
            opacity: 0.9;
        }

        /* ADDRESS BLOCK */
        .address-block {
            background: rgba(0,0,0,0.35);
            border-radius: 0.75rem;
            padding: 0.9rem;
            color: #fff;
            border: 1px dashed rgba(255,215,0,0.08);
            font-size: 0.98rem;
            line-height: 1.5;
        }

        .address-lines {
            margin-bottom: 0.6rem;
        }

        .btn-secondary {
            display: inline-block;
            margin-top: 0.6rem;
            padding: 10px 16px;
            border-radius: 999px;
            background: linear-gradient(135deg, rgba(255,215,0,0.08), rgba(255,215,0,0.12));
            color: #FFD700;
            border: 1px solid rgba(255,215,0,0.12);
            font-weight: 800;
            text-decoration: none;
            font-size: 0.95rem;
        }

        /* FOOTER NOTE */
        .donation-footer {
            margin-top: 1rem;
            font-size: 0.92rem;
            color: #ddd;
        }

        /* RESPONSIVE */
        @@media (max-width: 992px) {
            .donate-card {
                grid-template-columns: 1fr;
                text-align: left;
                padding: 1.6rem;
            }

            .donate-hero h1 {
                font-size: 2.4rem;
            }

            .donate-hero p {
                font-size: 1rem;
            }

            .donate-card {
                gap: 1rem;
            }
        }

        @@media (max-width: 768px) {
            .donate-hero h1 {
                font-size: 2rem;
            }

            .donate-content h2 {
                font-size: 1.6rem;
            }

            .bank-line {
                font-size: 0.92rem;
            }

            .copy-btn {
                padding: 6px 8px;
                font-size: 0.85rem;
            }
        }
    </style>
}



<section class="donate-section" aria-labelledby="donate-heading">
    <div class="donate-card" id="donate-card">
        <!-- LEFT: Story / Mission -->
        <div class="donate-content">
            <h2 id="donate-heading">Your gift changes lives</h2>
            <p>Alabaster Box Anointing Ministries depends on the faithful support of friends and family to run community feeding schemes, outreach programs, worship gatherings, and practical assistance for those in need. Every gift large or small helps share hope and practical care.</p>

            <!--   <img src="~/images/DonateMission.jpg" alt="Volunteers serving meals" class="donate-image" /> -->

            <div class="donate-note">
                <p class="small-muted">Need a receipt or have questions about giving? Please visit our <a href="/Contact" style="color:#FFD700; font-weight:700;">Contact</a> page so our team can assist you.</p>
            </div>

            <div class="donation-footer">
                <p class="small-muted">Thank you for partnering with us  we pray God multiplies every seed sown into abundant fruit.</p>
            </div>
        </div>

        <!-- RIGHT: Donate Options -->
        <div class="donate-options" id="donate-options">
            <!-- Monetary Option -->
            <div class="option-card" aria-labelledby="monetary-title">
                <h3 id="monetary-title">Monetary Donations / Bank Transfer</h3>
                <p>Make a secure bank transfer using the details below. After you transfer, please let us know via our Contact page so we can issue a receipt and say thank you.</p>

                <div class="bank-details" role="region" aria-label="Banking details">
                    <div class="bank-line"><span><strong>Bank:</strong> First National Bank</span></div>
                    <div class="bank-line">
                        <span><strong>Account Name:</strong> The Alabaster Box Anointing Ministries</span>
                        <button class="copy-btn" data-copy="The Alabaster Box Anointing Ministries" title="Copy account name">Copy</button>
                    </div>
                    <div class="bank-line">
                        <span><strong>Account Number:</strong> 62787787880</span>
                        <button class="copy-btn" data-copy="62787787880" title="Copy account number">Copy</button>
                    </div>
                    <div class="bank-line">
                        <span><strong>Branch Code:</strong> 250655</span>
                        <button class="copy-btn" data-copy="250655" title="Copy branch code">Copy</button>
                    </div>
                    <div class="bank-line"><span><strong>Account Type:</strong> Cheque account</span></div>
                </div>

                <p class="small-muted" style="margin-top:0.7rem;">Tip: Click <em>Copy</em> to quickly paste details into your internet banking or payments app.</p>

              
            </div>

            <!-- Physical Items Option -->
            <div class="option-card" aria-labelledby="items-title">
                <h3 id="items-title">Donations of Physical Items</h3>
                <p>If you'd like to send clothing, food, supplies, or other physical items, please post or courier them to the address below. Pack items securely and include a note with your contact details so we can acknowledge receipt.</p>

                <div class="address-block" role="region" aria-label="Postal address">
                    <div class="address-lines">
                        2 GARDEN CRESCENT<br />
                        NEW GERMANY<br />
                        PINETOWN<br />
                        KWA-ZULU NATAL<br />
                        3601
                    </div>

                       </div>

                <a class="btn-secondary" href="https://www.google.com/maps/search/2+Garden+Crescent+Pinetown+3601" target="_blank" rel="noopener noreferrer">Open in Maps</a>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    <script>
        // 
        const donateObserver = new IntersectionObserver(entries => {
            entries.forEach(entry => {
                if (entry.isIntersecting) entry.target.classList.add('visible');
            });
        }, { threshold: 0.15 });

        document.querySelectorAll('#donate-card').forEach(el => donateObserver.observe(el));

        // Copy to clipboard utility for bank details
        function handleCopy(text, btn) {
            if (!navigator.clipboard) {
                // 
                const ta = document.createElement('textarea');
                ta.value = text;
                document.body.appendChild(ta);
                ta.select();
                try { document.execCommand('copy'); }
                catch (e) { console.error('Copy failed', e); }
                ta.remove();
                showCopyFeedback(btn);
                return;
            }

            navigator.clipboard.writeText(text).then(() => {
                showCopyFeedback(btn);
            }).catch(err => {
                console.error('Could not copy text: ', err);
            });
        }

        function showCopyFeedback(btn) {
            const original = btn.textContent;
            btn.textContent = 'Copied!';
            btn.disabled = true;
            setTimeout(() => {
                btn.textContent = original;
                btn.disabled = false;
            }, 1800);
        }

        document.querySelectorAll('.copy-btn').forEach(btn => {
            btn.addEventListener('click', (e) => {
                const text = btn.getAttribute('data-copy') || '';
                handleCopy(text, btn);
            });
        });
    </script>
} *@
@* @{
    ViewData["Title"] = "Donate";
}

@section Styles {
<style>
:root{
  --glass-start: rgba(16,16,20,.55);
  --glass-end:   rgba(16,16,20,.38);
  --glass-border: rgba(255, 255, 255, 0.349);
  --glass-shadow: 0 40px 90px rgba(0,0,0,.45);
  --gold: #FFD700;
}

/* Base */
*{box-sizing:border-box;margin:0;padding:0}
html,body{
  font-family:'Poppins','Segoe UI',system-ui,-apple-system,sans-serif;
  background:#08090c; color:#fff; min-height:100vh; overflow-x:hidden;
}
a{color:inherit;text-decoration:none}
img{max-width:100%;display:block}

/* Ambient backdrops */
.page-aurora{
  position:fixed; inset:-20vmax -10vmax -10vmax -10vmax; pointer-events:none; z-index:0;
  filter:blur(40px) saturate(120%);
  background:
    radial-gradient(40vmax 40vmax at 15% 20%, rgba(255,215,0,.13), transparent 60%),
    radial-gradient(35vmax 35vmax at 85% 20%, rgba(0,114,255,.12), transparent 60%),
    radial-gradient(45vmax 45vmax at 50% 80%, rgba(0,255,188,.10), transparent 60%);
  opacity:.9;
}
.page-grain::before{
  content:""; position:fixed; inset:0; z-index:0; pointer-events:none;
  background:url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="140" height="140" viewBox="0 0 140 140"><filter id="n"><feTurbulence baseFrequency="0.8" numOctaves="2" stitchTiles="stitch"/></filter><rect width="100%" height="100%" filter="url(%23n)" opacity="0.035"/></svg>');
  mix-blend-mode:soft-light;
}

/* Layout wrapper */
.about-wrap{
  position:relative; z-index:1;
  display:grid; gap:clamp(2rem,4vw,3rem);
  padding:clamp(2.5rem,5vw,5rem) 1.25rem clamp(4rem,7vw,6rem);
  max-width:1400px; margin-inline:auto;
}

/* Heading */
.page-head{display:grid;gap:.6rem;justify-items:center;text-align:center}
.page-head h1{
  font-size:clamp(2.4rem,5.6vw,4.4rem); font-weight:900; line-height:1.05; letter-spacing:.3px; text-transform:uppercase;
  background:linear-gradient(90deg,var(--gold), #ffffff 45%, var(--gold));
  background-size:200% auto; color:transparent; -webkit-background-clip:text; background-clip:text;
  animation:aboutShimmer 8s linear infinite; text-shadow:0 0 18px rgba(255,255,255,.18);
}
.page-head .tagline{font-size:clamp(1rem,1.6vw,1.2rem); letter-spacing:.6px; opacity:.95}
@@keyframes aboutShimmer{from{background-position:0% 50%}to{background-position:200% 50%}}

/* Intro */
.hero{display:grid;gap:.9rem;justify-items:center;text-align:center}
.hero p{max-width:900px;opacity:.96;font-size:clamp(.98rem,1.4vw,1.05rem);line-height:1.8}

/* Glass cards */
.panel-card{
  border-radius:28px; border:1px solid var(--glass-border);
  background:linear-gradient(180deg,var(--glass-start),var(--glass-end));
  backdrop-filter:blur(14px) saturate(150%); -webkit-backdrop-filter:blur(14px) saturate(150%);
  box-shadow:var(--glass-shadow);
  padding:clamp(1.25rem,3.2vw,2.2rem);
  opacity:0; transform:translateY(20px); transition:opacity .8s ease, transform .8s ease;
}
.panel-card.visible{opacity:1; transform:translateY(0)}

.section-title{
  font-size:clamp(1.5rem,2.4vw,2.2rem); font-weight:900; line-height:1.1; letter-spacing:.3px; text-transform:uppercase;
  background:linear-gradient(90deg,var(--gold), #ffffff 45%, var(--gold));
  background-size:200% auto; color:transparent; -webkit-background-clip:text; background-clip:text;
  animation:aboutShimmer 8s linear infinite; margin-bottom:.65rem;
}
.section-body{opacity:.96; line-height:1.8; margin-top:.35rem; font-size:1.02rem}

/* Reusable info blocks */
.note,.address-block,.bank-details{
  background:rgba(0,0,0,.35); border-radius:12px; padding:.9rem;
  border:1px dashed rgba(255,215,0,.12); color:#fff; font-size:.98rem; line-height:1.55; margin-top:.9rem;
}

/* Bank rows */
.bank-line{display:flex;justify-content:space-between;align-items:center;gap:.8rem;margin-bottom:.5rem;font-size:.98rem}
.bank-line strong{color:#ffd;font-weight:700}
.small-muted{font-size:.88rem;color:#ddd;opacity:.9}

/* Buttons */
.copy-btn{
  display:inline-flex;align-items:center;gap:.5rem;padding:6px 10px;font-size:.9rem;border-radius:999px;
  background:rgba(255,215,0,.95);color:#000;border:none;cursor:pointer;font-weight:700;
  box-shadow:0 8px 20px rgba(255,215,0,0.12);
}
.btn-secondary{
  display:inline-block;margin-top:.6rem;padding:10px 16px;border-radius:999px;
  background:linear-gradient(135deg,rgba(255,215,0,0.08),rgba(255,215,0,0.12));
  color:#FFD700;border:1px solid rgba(255,215,0,0.12);font-weight:800;font-size:.95rem;text-decoration:none;
}

/* Chips, lists & helpers (used in Physical Items) */
.item-grid{display:grid;gap:.5rem;grid-template-columns:repeat(3,minmax(0,1fr))}
@@media (max-width:900px){.item-grid{grid-template-columns:repeat(2,1fr)}}
@@media (max-width:520px){.item-grid{grid-template-columns:1fr}}
.chip{
  display:inline-block;padding:.5rem .9rem;border-radius:999px;font-weight:700;letter-spacing:.2px;text-align:center;
  border:1px solid rgba(255,215,0,.22); background:linear-gradient(180deg,rgba(255,255,255,.06),rgba(255,255,255,.02));
  box-shadow:0 8px 22px rgba(0,0,0,.25);
}
.soft-divider{height:1px;margin:.9rem 0 1.1rem;background:linear-gradient(90deg,transparent,rgba(255,255,255,.18),transparent)}
.two-col{display:grid;gap:clamp(.8rem,2vw,1.2rem);grid-template-columns:1fr 1fr}
@@media (max-width:900px){.two-col{grid-template-columns:1fr}}
.mini-title{font-size:1.02rem;font-weight:800;letter-spacing:.3px;margin-bottom:.4rem}
.gold-list{list-style:none;padding-left:0;margin:0}
.gold-list li{position:relative;padding-left:1.35rem;margin:.4rem 0;opacity:.95}
.gold-list li::before{content:"✓";position:absolute;left:0;top:0;color:var(--gold);font-weight:900}

.address-card{padding:1.1rem}
.address-header{
  font-weight:900;letter-spacing:.3px;margin-bottom:.35rem;
  background:linear-gradient(90deg,var(--gold), #ffffff 45%, var(--gold));
  -webkit-background-clip:text;background-clip:text;color:transparent;background-size:200% auto;animation:aboutShimmer 8s linear infinite;
}
.address-lines{
  margin:.1rem 0 .4rem;font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,"Liberation Mono",monospace;white-space:pre-wrap
}
.btn-row{display:flex;gap:.6rem;flex-wrap:wrap;align-items:center}
</style>
}

<div class="page-aurora"></div>
<div class="page-grain"></div>

<div class="about-wrap">
  <!-- Heading -->
  <header class="page-head" aria-labelledby="donate-title">
    <h1 id="donate-title">Donate</h1>
    <div class="tagline">Christ-centred • Generosity • Outreach • Impact</div>
  </header>

  <!-- Intro -->
  <section class="hero" aria-label="Donate intro">
    <p>
      Your generosity helps us feed families, reach communities, host worship gatherings, and bring practical care and hope in Jesus’ name.
      Thank you for partnering with the mission.
    </p>
  </section>

  <!-- Card 1: Story / Mission -->
  <section class="panel-card" id="card-story" aria-label="Ministry impact">
    <h2 class="section-title">Your gift changes lives</h2>
    <div class="section-body">
      <p>
        Alabaster Box Anointing Ministries depends on the faithful support of friends and family to run community
        feeding schemes, outreach programs, worship gatherings, and practical assistance for those in need.
        Every gift large or small helps share hope and practical care.
      </p>

      <div class="note">
        <p class="small-muted">
          Need a receipt or have questions about giving? Please visit our
          <a href="/Contact" style="color:#FFD700;font-weight:700;">Contact</a>
          page so our team can assist you.
        </p>
      </div>

      <p class="small-muted" style="margin-top:.9rem;">
        Thank you for partnering with us — we pray God multiplies every seed sown into abundant fruit.
      </p>
    </div>
  </section>

  <!-- Card 2: Monetary / Bank (now stacked) -->
  <section class="panel-card" id="card-bank" aria-labelledby="monetary-title">
    <h2 id="monetary-title" class="section-title">Monetary Donations / Bank Transfer</h2>
    <div class="section-body">
      <p>
        Make a secure bank transfer using the details below. After you transfer, please let us know via our Contact page
        so we can issue a receipt and say thank you.
      </p>

      <div class="bank-details" role="region" aria-label="Banking details">
        <div class="bank-line"><span><strong>Bank:</strong> First National Bank</span></div>
        <div class="bank-line">
          <span><strong>Account Name:</strong> The Alabaster Box Anointing Ministries</span>
          <button class="copy-btn" data-copy="The Alabaster Box Anointing Ministries" title="Copy account name">Copy</button>
        </div>
        <div class="bank-line">
          <span><strong>Account Number:</strong> 62787787880</span>
          <button class="copy-btn" data-copy="62787787880" title="Copy account number">Copy</button>
        </div>
        <div class="bank-line">
          <span><strong>Branch Code:</strong> 250655</span>
          <button class="copy-btn" data-copy="250655" title="Copy branch code">Copy</button>
        </div>
        <div class="bank-line"><span><strong>Account Type:</strong> Cheque account</span></div>
      </div>

      <p class="small-muted" style="margin-top:.7rem;">
        Tip: Click <em>Copy</em> to quickly paste details into your internet banking or payments app.
      </p>
    </div>
  </section>

  <!-- Card 3: Physical Items (stacked beneath banking) -->
  <section class="panel-card" id="card-items" aria-labelledby="items-title">
    <h2 id="items-title" class="section-title">Donations of Physical Items</h2>
    <div class="section-body">
      <div class="item-grid" aria-label="Popular item categories">
        <span class="chip">Non-perishable Foods</span>
        <span class="chip">Warm Clothing</span>
        <span class="chip">Hygiene Packs</span>
        <span class="chip">Baby Supplies</span>
        <span class="chip">School Stationery</span>
        <span class="chip">Blankets</span>
      </div>

      <div class="soft-divider" role="presentation"></div>

      <div class="two-col">
        <div>
          <h3 class="mini-title">What helps most</h3>
          <ul class="gold-list">
            <li>Canned food (beans, tuna, soup), maize meal, long-life milk</li>
            <li>Toiletries: soap, toothpaste, sanitary pads, deodorant</li>
            <li>Gently-used winter wear &amp; shoes (clean &amp; bagged)</li>
            <li>Baby: nappies, wipes, formula (sealed), blankets</li>
            <li>Stationery: exam pads, pens, pencils, crayons</li>
          </ul>
        </div>
        <div>
          <h3 class="mini-title">Courier &amp; packing tips</h3>
          <ul class="gold-list">
            <li>Label each box with a short summary (e.g., “Hygiene x20”).</li>
            <li>Include your name &amp; contact so we can acknowledge receipt.</li>
            <li>Place liquids in sealed zip bags; cushion fragile items.</li>
            <li>For bulk donations, please notify us via the <a href="/Contact">Contact</a> page.</li>
          </ul>
        </div>
      </div>

      <div class="address-block address-card" role="region" aria-label="Postal / Courier address">
        <div class="address-header">Ship / Drop-off Address</div>
        <pre class="address-lines" aria-label="Full address">2 GARDEN CRESCENT
NEW GERMANY
PINETOWN
KWA-ZULU NATAL
3601</pre>

          <a class="btn-secondary"
             href="https://www.google.com/maps/search/2+Garden+Crescent+Pinetown+3601"
             target="_blank" rel="noopener noreferrer">
            Open in Maps
          </a>
        </div>
      </div>
    </div>
  </section>
</div>

@section Scripts {
<script>
// Reveal cards on enter
const reveal = (el, threshold=.2) => {
  const io = new IntersectionObserver((entries, obs) => {
    entries.forEach(e => { if (e.isIntersecting) { e.target.classList.add('visible'); obs.unobserve(e.target); } });
  }, { threshold });
  io.observe(el);
};
document.addEventListener('DOMContentLoaded', () => {
  document.querySelectorAll('.panel-card').forEach(c => reveal(c, .2));
});

// Copy to clipboard
function handleCopy(text, btn){
  if(!navigator.clipboard){
    const ta=document.createElement('textarea'); ta.value=text; document.body.appendChild(ta); ta.select();
    try{ document.execCommand('copy'); }catch(e){ console.error('Copy failed', e); }
    ta.remove(); showCopyFeedback(btn); return;
  }
  navigator.clipboard.writeText(text).then(()=>showCopyFeedback(btn)).catch(err=>console.error('Could not copy text:', err));
}
function showCopyFeedback(btn){
  const original=btn.textContent; btn.textContent='Copied!'; btn.disabled=true;
  setTimeout(()=>{ btn.textContent=original; btn.disabled=false; }, 1800);
}
document.querySelectorAll('.copy-btn').forEach(btn=>{
  btn.addEventListener('click', ()=>handleCopy(btn.getAttribute('data-copy')||'', btn));
});
</script>
} *@


@{
    ViewData["Title"] = "Donate";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css"/>

@section Styles {
<style>
:root{
  --bg:#08090c;
  --ink:#f2f2f2;
  --muted:#cfcfd6;
  --glass-1: rgba(18,18,22,.55);
  --glass-2: rgba(18,18,22,.35);
  --glass-brd: rgba(255,255,255,.14);
  --gold:#FFD700;
  --gold-ink:#181818;
  --shadow:0 24px 60px rgba(0,0,0,.55);
}

/* ===== Base ===== */
html, body{
  margin:0;
  font-family:'Poppins','Segoe UI',system-ui,-apple-system,sans-serif;
  color:var(--ink);
  min-height:100vh; overflow-x:hidden;
  background: var(--bg) url('../images/cross7.png') center -10% / cover no-repeat fixed;
}

/* ===== Title (matches site look) ===== */
.hero-title{
  text-align:center;
  padding: clamp(100px, 12vh, 160px) 20px 50px;
  position:relative; z-index:1;
}
.hero-title h1{
  font-size: clamp(2.2rem, 5vw, 3.6rem);
  font-weight:900; text-transform:uppercase; letter-spacing:.12em;
  background: linear-gradient(90deg, var(--gold), #fff 45%, var(--gold));
  -webkit-background-clip:text; background-clip:text; color:transparent;
  text-shadow:0 0 34px rgba(255,255,255,.32);
  background-size:200% auto; animation: shimmer 7s linear infinite;
}
.hero-title p{
  font-size:1.05rem; color:#e6e6ef; opacity:.92;
  max-width:760px; margin: 10px auto 0;
}
@@keyframes shimmer{ from{background-position:0% 50%} to{background-position:200% 50%} }

/* ===== Page ===== */
.page-wrap{ position:relative; padding: 40px 18px 100px; }

/* ===== Glass Card ===== */
.form-card{
  width:min(980px, 94vw);
  margin:0 auto;
  padding: clamp(18px, 2.2vw, 26px);
  border-radius:22px;
  border:1px solid var(--glass-brd);
  background: linear-gradient(180deg, var(--glass-1), var(--glass-2));
  backdrop-filter: blur(14px) saturate(140%);
  -webkit-backdrop-filter: blur(14px) saturate(140%);
  box-shadow: var(--shadow);
  opacity:0; transform: translateY(18px);
  transition: opacity .6s ease, transform .6s ease;
}
.form-card.visible{ opacity:1; transform:none; }

h2.section-title{
  margin:0 0 2px 0;
  text-align:center;
  font-size: clamp(1.4rem, 2.2vw, 2rem);
  font-weight:900;
  background: linear-gradient(90deg, var(--gold), #ffffff 45%, var(--gold));
  -webkit-background-clip:text; background-clip:text; color:transparent;
  text-shadow:0 0 18px rgba(255,255,255,.18);
}
.subtitle{
  text-align:center; margin-top:6px; color:#dcdce5; opacity:.9; font-size:.98rem;
}

/* trust chips */
.trust-row{
  display:flex; gap:.5rem; justify-content:center; flex-wrap:wrap;
  margin: 10px 0 4px;
}
.trust-chip{
  font-size:.72rem; font-weight:900; letter-spacing:.08em;
  color:#F29A2E; text-transform:uppercase;
  padding:.35rem .7rem; border-radius:999px;
  background: rgba(242,154,46,.1); border: 1px solid rgba(242,154,46,.35);
}

/* divider */
.hr{ height:1px; margin: 14px 0 12px;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,.18), transparent); border:0; }

/* ===== Form grid ===== */
.form-grid{ display:grid; gap:14px; grid-template-columns: 1fr; }
@@media (min-width: 860px){
  .form-grid{ grid-template-columns: 1fr 1fr; }
  .col-span-2{ grid-column:1 / -1; }
}
label{ font-weight:800; font-size:.94rem; color:#efefef; }

/* input shell with gold-only glow */
.input-wrap{
  position:relative; display:flex; align-items:center; gap:10px;
  border:1px solid rgba(255,255,255,.14);
  background: linear-gradient(180deg, rgba(255,255,255,.08), rgba(255,255,255,.03));
  border-radius:14px; padding:10px 12px;
  transition: border-color .25s ease, box-shadow .25s ease, background .25s ease;
}
.input-wrap:focus-within{
  border-color: rgba(255,215,0,.55)!important;
  box-shadow: 0 0 0 6px rgba(255,215,0,.10)!important;
  background: linear-gradient(180deg, rgba(255,255,255,.12), rgba(255,255,255,.05));
}
.input-icon{ font-size:1.05rem; opacity:.9; }

.form-control,
select.form-control{
  flex:1; border:0; outline:0; background:transparent!important;
  color:var(--ink)!important; font-size:.98rem; padding:8px 4px;
  caret-color: var(--gold);
}
textarea.form-control{ resize:vertical; min-height:110px; }
.form-control:focus{ outline:none!important; box-shadow:none!important; }

/* custom select caret */
.select-wrap{ position:relative; }
.select-wrap .caret{
  position:absolute; right:12px; pointer-events:none; opacity:.9;
}

/* ===== File input (custom, gold) ===== */
.file-wrap{ position:relative; display:flex; align-items:center; gap:10px; }
input[type="file"].file-control{
  flex:1; border:0; outline:0; background:transparent; padding:0; color:#ddd;
}
input[type="file"].file-control::-webkit-file-upload-button{
  -webkit-appearance: button; appearance: none;
  border:1px solid rgba(255,215,0,.45);
  background: linear-gradient(135deg, rgba(255,215,0,.12), rgba(255,215,0,.20));
  color:#FFD700; font-weight:800; border-radius:999px;
  padding:.45rem .9rem; margin-right:.6rem; cursor:pointer;
}
input[type="file"].file-control::file-selector-button{
  border:1px solid rgba(255,215,0,.45);
  background: linear-gradient(135deg, rgba(255,215,0,.12), rgba(255,215,0,.20));
  color:#FFD700; font-weight:800; border-radius:999px;
  padding:.45rem .9rem; margin-right:.6rem; cursor:pointer;
}
input[type="file"].file-control:focus{ outline:none; }

/* hints */
.small-muted{ font-size:.88rem; color:#ddd; opacity:.9; }

/* ===== Bank details box ===== */
.bank-details{
  background: rgba(0,0,0,.35);
  border:1px dashed rgba(255,215,0,.18);
  border-radius:12px; padding:.95rem; color:#fff; margin-top:.6rem;
}
.bank-line{
  display:flex; justify-content:space-between; align-items:center; gap:.8rem;
  font-size:.98rem; margin:.45rem 0;
}
.bank-line strong{ color:#ffd; font-weight:800; }
.copy-btn{
  display:inline-flex; align-items:center; gap:.45rem;
  padding:.45rem .75rem; border-radius:999px; font-weight:800; font-size:.9rem;
  background: rgba(255,215,0,.95); color:#000; border:none; cursor:pointer;
  box-shadow:0 8px 20px rgba(255,215,0,.18);
}

/* ===== Actions ===== */
.actions{ margin-top:14px; display:grid; gap:12px; }
.btn-primary{
  display:inline-flex; align-items:center; justify-content:center; gap:10px;
  background: linear-gradient(135deg, var(--gold), #FFB700, var(--gold));
  color: var(--gold-ink);
  border: 1px solid rgba(255,215,0,.5);
  border-radius: 12px;
  font-weight:900; font-size:.98rem;
  padding:12px 16px; width:100%;
  box-shadow:0 10px 26px rgba(255,215,0,.28);
  transition: transform .18s ease, box-shadow .18s ease, filter .18s ease;
}
.btn-primary:hover{ transform:translateY(-1px); box-shadow:0 16px 38px rgba(255,215,0,.38); filter:saturate(1.05); }

/* bottom link button */
.btn-alt{
  display:inline-flex; align-items:center; justify-content:center; gap:12px;
  background: linear-gradient(135deg, rgba(255,255,255,.10), rgba(255,255,255,.04));
  color:#fff; border:1px solid rgba(255,255,255,.18);
  border-radius:12px; font-weight:800; font-size:.96rem;
  padding:12px 16px; width:100%;
  box-shadow:0 10px 24px rgba(0,0,0,.28);
  transition: transform .18s ease, box-shadow .18s ease, filter .18s ease;
  text-decoration:none;
}
.btn-alt:hover{ transform:translateY(-1px); box-shadow:0 16px 36px rgba(0,0,0,.38); }

/* tiny note */
.meta-note{ text-align:center; font-size:.9rem; color:var(--muted); }

/* remove default blue glow on inputs (Safari/Chrome autofill) */
input:-webkit-autofill,
textarea:-webkit-autofill,
select:-webkit-autofill{
  -webkit-box-shadow:0 0 0 1000px transparent inset !important;
  -webkit-text-fill-color: var(--ink) !important;
}
</style>
}

<!-- ===== Title ===== -->
<section class="hero-title">
  <h1>Donate Now</h1>
  <p>Make a difference today. Your generosity fuels outreach, feeding schemes, and practical care in Jesus’ name.</p>
</section>

<!-- ===== Form ===== -->
<div class="page-wrap">
  <div class="form-card" id="donateCard">
    <h2 class="section-title">Donation Details</h2>
    <div class="trust-row">
      <span class="trust-chip">Secure</span>
      <span class="trust-chip">Fast</span>
      <span class="trust-chip">Trusted</span>
    </div>
    <p class="subtitle">Fill in the form below to submit your donation information.</p>
    <hr class="hr"/>

    <form asp-controller="Donations" asp-action="Create" method="post" enctype="multipart/form-data" novalidate>
      <div class="form-grid">
        <!-- Full Name -->
        <div class="col-span-2">
          <label>Full Name</label>
          <div class="input-wrap">
            <i class="bi bi-person input-icon"></i>
            <input name="FullName" class="form-control" required placeholder="Enter your full legal name"/>
          </div>
          <div class="small-muted">Please enter your full legal name.</div>
        </div>

        <!-- Donation Type (spinner) -->
        <div class="col-span-2">
          <label>Donation Type</label>
          <div class="input-wrap select-wrap">
            <i class="bi bi-box-seam input-icon"></i>
            <select name="DonationType" id="DonationType" class="form-control" required>
              <option value="" disabled selected>Select a type</option>
              <option value="Money">Money (EFT)</option>
              <option value="Food & Groceries">Food &amp; Groceries</option>
              <option value="Clothing & Blankets">Clothing &amp; Blankets</option>
              <option value="Hygiene Packs">Hygiene Packs</option>
              <option value="Baby Supplies">Baby Supplies</option>
              <option value="School Stationery">School Stationery</option>
              <option value="Other Items">Other Items</option>
            </select>
            <span class="caret"><i class="bi bi-caret-down-fill"></i></span>
          </div>
        </div>

        <!-- Amount (shown only if Money) -->
        <div class="col-span-2" id="amountRow" style="display:none;">
          <label>Donation Amount</label>
          <div class="input-wrap">
            <i class="bi bi-cash-coin input-icon"></i>
            <input name="Amount" type="number" step="0.01" min="0" class="form-control" placeholder="Enter amount (e.g., 500.00)"/>
          </div>
          <div class="small-muted">Only required for money donations.</div>
        </div>

        <!-- Contact details -->
        <div class="col-span-2">
          <label>Contact Details</label>
          <div class="input-wrap">
            <i class="bi bi-telephone input-icon"></i>
            <input name="Contact" class="form-control" placeholder="Phone, email, etc." required/>
          </div>
        </div>

        <!-- Date -->
        <div>
          <label>Date</label>
          <div class="input-wrap">
            <i class="bi bi-calendar-event input-icon"></i>
            <input name="Date" type="date" class="form-control" required/>
          </div>
        </div>

        <!-- Optional Notes -->
        <div>
          <label>Notes (optional)</label>
          <div class="input-wrap">
            <i class="bi bi-chat-square-text input-icon"></i>
            <input name="Notes" class="form-control" placeholder="Any extra info (e.g., reference used)"/>
          </div>
        </div>

        <!-- Proof of Payment (optional) -->
        <div class="col-span-2">
          <label>Proof of Payment (optional)</label>
          <div class="input-wrap file-wrap">
            <i class="bi bi-paperclip input-icon"></i>
            <input name="ProofFile" id="ProofFile" type="file" accept="image/*,.pdf" class="file-control"/>
          </div>
          <div class="small-muted">You can also email your proof later via the <a href="/Contact">Contact</a> page.</div>
        </div>

        <!-- Banking details (Auto show for Money) -->
        <div class="col-span-2" id="bankBlock" style="display:none;">
          <div class="bank-details" role="region" aria-label="Banking details">
            <div class="bank-line">
              <span><strong>Bank:</strong> First National Bank</span>
            </div>
            <div class="bank-line">
              <span><strong>Account Name:</strong> The Alabaster Box Anointing Ministries</span>
              <button class="copy-btn" type="button" data-copy="The Alabaster Box Anointing Ministries"><i class="bi bi-clipboard"></i> Copy</button>
            </div>
            <div class="bank-line">
              <span><strong>Account Number:</strong> 62787787880</span>
              <button class="copy-btn" type="button" data-copy="62787787880"><i class="bi bi-clipboard"></i> Copy</button>
            </div>
            <div class="bank-line">
              <span><strong>Branch Code:</strong> 250655</span>
              <button class="copy-btn" type="button" data-copy="250655"><i class="bi bi-clipboard"></i> Copy</button>
            </div>
            <div class="bank-line">
              <span><strong>Account Type:</strong> Cheque account</span>
            </div>
            <div class="small-muted">Reference suggestion: <em>Your Name - Donation</em>. After transferring, please let us know via <a href="/Contact">Contact</a> so we can send a receipt.</div>
          </div>
        </div>
      </div>

      <div class="actions">
        <button type="submit" class="btn-primary">
          <i class="bi bi-send"></i> Submit Donation
        </button>
        <div class="meta-note">Your generosity fuels real impact. Thank you!</div>
      </div>
    </form>
  </div>
</div>

@section Scripts {
<script>
// Reveal card
const card = document.getElementById('donateCard');
requestAnimationFrame(()=> card?.classList.add('visible'));

// Toggle Money fields + bank details
const typeSel = document.getElementById('DonationType');
const amountRow = document.getElementById('amountRow');
const bankBlock = document.getElementById('bankBlock');

function updateType(){
  const isMoney = typeSel?.value === 'Money';
  if(amountRow) amountRow.style.display = isMoney ? '' : 'none';
  if(bankBlock) bankBlock.style.display = isMoney ? '' : 'none';
}
typeSel?.addEventListener('change', updateType);
updateType();

// Copy buttons
function copyText(text, btn){
  if(!navigator.clipboard){
    const ta=document.createElement('textarea'); ta.value=text; document.body.appendChild(ta);
    ta.select(); try{document.execCommand('copy')}catch(_){}
    ta.remove(); flash(btn); return;
  }
  navigator.clipboard.writeText(text).then(()=>flash(btn));
}
function flash(btn){
  if(!btn) return;
  const prev=btn.innerHTML;
  btn.innerHTML = '<i class="bi bi-check2"></i> Copied!';
  btn.disabled = true;
  setTimeout(()=>{ btn.innerHTML=prev; btn.disabled=false; }, 1200);
}
document.querySelectorAll('.copy-btn').forEach(b=>{
  b.addEventListener('click', ()=> copyText(b.getAttribute('data-copy')||'', b));
});
</script>
}
